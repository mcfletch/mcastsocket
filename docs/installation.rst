.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install mcastsocket

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mcastsocket
    $ pip install mcastsocket
